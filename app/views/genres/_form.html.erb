<%= form_for(@genre) do |f| %>
  <% if @genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

      <ul>
      <% @genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field form-group">
    <% f.label :genre_id %>
     <%= f.collection_select :genre_id, Genre.order('name'), :id, :name, {prompt: "select a genre"}, {class: "form-control"} %>
  </div>
  <div class="actions form-group">
    <%= f.submit %>
  </div>
<% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<!-- <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control"%>
  </div>
  <div class="field form-group">
    <%= f.label :genre_id %><br>
    <%= f.collection_select :genre_id, Genre.order('name'), :id, :name, {prompt: "select a genre"}, {class: "form-control"} %>
  </div>
  <div class="actions form-group">
    <%= f.submit %>
  </div>
<% end %>
 -->